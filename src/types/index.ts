export type SidebarItem = {
    id: string;
    label: string;
    icon: JSX.Element;
    url: string;
};

export type User = {
    id: string;
    name: string;
    email: string;
    contactNumber: string;
    address: string;
};
